###################################
# OAuth2 Configuration
###################################
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET
spring.security.oauth2.client.registration.google.scope=openid,profile,email

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v2/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=id

###################################
# Database Configuration
# spring.datasource.url=jdbc:postgresql://<db_host>:<port>/postgres
# spring.datasource.username=postgres
# spring.datasource.password=<db_password>
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

# Supabase PostgreSQL Configuration (Direct Connection)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.knzqgamooqiuesfmmqvr.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:faisalportfoli}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

###################################
# JWT Configuration
###################################
jwt.secret=${JWT_SECRET:my-super-secret-placeholder-for-dev}
jwt.expiration=86400000

###################################
# Server Configuration
###################################
server.port=8080
server.servlet.context-path=/

###################################
# Application Configuration
###################################
spring.application.name=portfolio-backend
logging.level.root=INFO
logging.level.com.example.portfolio=DEBUG